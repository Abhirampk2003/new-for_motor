const display = require('./display');
const backend = require('./backend');

// Initialize frontend screen with start button
display.showStartButton();

// Simulate button click (this would be replaced by actual touch event handling)
display.onStartButtonClick();
